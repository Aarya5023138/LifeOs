{
  "name": "lifeos",
  "version": "1.0.0",
  "private": true,
  "description": "LifeOS Productivity Suite",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mongoose": "^8.7.0",
    "multer": "^2.1.1"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install:server": "cd server && npm install --omit=dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install"
  }
}
